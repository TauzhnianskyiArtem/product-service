package com.iprody.product.service.rest.validation.group;

/**
 * The validation group indicates the validation annotations that will work if this group is defined during validation.
 */
public interface CreateActionGroup {
}
